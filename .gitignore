node_modules/
dist/
coverage/
*.local
.env
.env.*
.DS_Store
Thumbs.db

# Duplicate skills mirror (canonical copy lives in .claude/skills)
.agents/

# Playwright
playwright-report/
test-results/

tmp/
# Local per-machine Claude Code launch config (not shared)
.claude/launch.json

# Session keepsakes (raw transcripts, per-machine mementos) — kept locally, never tracked
docs/souvenirs/
